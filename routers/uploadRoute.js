//JB: Disclaimer: It is just me playing around with stuff

/* import express from "express";
import { upload } from "../middlewares/uploadFiles.js";

const router = express.Router();

//JB: uses upload (multer middleware) and the method single() to read the file and use this information for the controller "uploadPicture"
router.post("/upload", upload.single("picture"), uploadPicture);

export default router; */